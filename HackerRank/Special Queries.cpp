#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    // --- your code here ---
    int t;
    cin >> t;
    while (t--)
    {
        int command;
        cin >> command;
        if (command == '0')
        {
            string Name;
            cin >> Name;
        }
        else if (command == '1')
        {
            
        }
        
        
    }
    
    return 0;
}
