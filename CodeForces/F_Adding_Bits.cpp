#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    // --- your code here ---
    unsigned int a, b;
    cin >> a >> b;
    cout << (a ^ b) << "\n";

    return 0;
}
