#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    // --- your code here ---
    string s;
    cin >> s;
    stringstream ss(s);
    string word;
    ss >> word;
    cout << word << " ";
    return 0;
}
