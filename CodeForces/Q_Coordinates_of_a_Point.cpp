#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    // --- your code here ---
    float X, Y;
    cin >> X >> Y;
    if (X == 0 && Y == 0)
    {
        cout << "Origem" << endl;
    }
    else if (X = 0)
    {
        cout << "Eixo X" << endl;
    }
    else if (Y == 0)
    {
        cout << "Eixo Y" << endl;
    }
    else if (condition)
    {
        /* code */
    }
    

    return 0;
}
