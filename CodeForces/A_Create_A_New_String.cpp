#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    // --- your code here ---
    string s, t;
    cin >> s >> t;
    cout << s.size() << " " << t.size() << endl;
    cout << s << " " << t << endl;
    return 0;
}
