#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    // --- your code here ---
    int t;
    cin >> t;
    while (t--)
    {
        long long x, y, a;
        cin >> x >> y >> a;

        int deepth = a + 0.5;
        bool isLittleB = true;
        while (deepth <= x + y)
        {
            
        }
        


        if (isLittleB)
        {
            cout << "NO" << endl;
        }
        else 
        {
            cout << "YES" << endl;
        }
        
    }

    return 0;
}
