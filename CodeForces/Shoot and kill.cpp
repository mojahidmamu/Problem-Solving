#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    // --- your code here ---
    int n;
    cin >> n;
    while (n--)
    {
        int x, y;
        cin >> x >> y;
    }

    int q;
    cin >> q;
    while (q--)
    {
        int L, R;
        cin >> L >> R;
    }

    return 0;
}
