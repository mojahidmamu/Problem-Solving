#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    // --- your code here ---
    int n, m;
    cin >> n >> m;
    long long arr1[n];
    for (int i = 0; i < n; i++)
    {
        cin >> arr1[i];
    }
    //  
    long long arr2[m];
    for (int i = 0; i < m; i++)
    {
        cin >> arr2[i];
    }

    return 0;
}
